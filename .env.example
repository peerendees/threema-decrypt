# Bearer-Token, das Aufrufer (n8n) im Authorization-Header senden müssen.
# Muss identisch zu $env.DECRYPT_API_TOKEN in n8n sein.
DECRYPT_API_TOKEN=

# Optional: erlaubte Browser-Origin für CORS. Für reine Server-zu-Server-Aufrufe leer lassen.
ALLOWED_ORIGIN=

# Mistral API – wird von Supabase aus aufgerufen (nicht von n8n/Hostinger).
MISTRAL_API_KEY=
# Optional, Standard: pixtral-12b-2409
# MISTRAL_VISION_MODEL=

# Beirat-Bot (*BERENTB, E2E) — Callback-Empfaenger /api/beirat-callback + send_beirat
THREEMA_GATEWAY_ID_BERENTB=*BERENTB
THREEMA_SECRET_BERENTB=
THREEMA_PRIVATE_KEY_BERENTB=
# optional: N8N_BEIRAT_WEBHOOK (Default zeigt auf den aktiven Beirat-Webhook)
# optional: BERENTB_ERLAUBTE_ABSENDER (Threema-IDs, kommagetrennt; Default BUMFMZ39)
# optional: N8N_SKILL_WEBHOOK  (Default zeigt auf den Skill Executor — Kommando "idee")
